package tris.papa_applition;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class ModeOfGuidance extends AppCompatActivity {//implements View.OnClickListener {
//Button Call,Vediocall,Clientplace;
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_mode_of_guidance);

/*
        Call=findViewById(R.id.call);
        Vediocall=findViewById(R.id.vediocall);
        Clientplace=findViewById(R.id.clientplace);

        Call.setOnClickListener(this);
        Vediocall.setOnClickListener(this);
        Clientplace.setOnClickListener(this);

    }

    @Override
    public void onClick(View v) {
        if (v==Call)
        {
            Intent i=new Intent(this,MainActivity.class);
            startActivity(i);
        }
        else if (v==Vediocall)
        {
            Intent i=new Intent(this,MainActivity.class);
            startActivity(i);
        }
        else if (v==Clientplace)
        {
            Intent i=new Intent(this,MainActivity.class);
            startActivity(i);
        }

*/

    }
}
